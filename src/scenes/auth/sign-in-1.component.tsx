import React from 'react';
import ContentView from '../../layouts/auth/sign-in-1';

export const SignIn1Screen = ({ props,navigation }): React.ReactElement => (
  <ContentView props={props} navigation={navigation} />
);
